@using semana4.ViewModels;
@model LoginVM

@{
  
}
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/StyleSheetContForm.css">
</head>
<body>
    <div class="row d-flex align-items-center justify-content-center">
        <div class="wrapper">
            <form asp-action="Login" method="post" asp-route-returnurl="@ViewData["ReturnUrl"]">
                <h2 class="form-login">Login</h2>
                <div asp-validation-summary="ModelOnly" class="text-end"></div>
                <div class="mb-1">
                    <label asp-for="Username" class="control-label"></label>
                    <input asp-for="Username" class="form-control" />
                    <span asp-validation-for="Username" class="validation-message"></span>
                </div>
                <div class="mb-1">
                    <label asp-for="Password" class="control-label"></label>
                    <input asp-for="Password" class="form-control" />
                    <span asp-validation-for="Password" class="validation-message"></span>
                </div>
                <div class="mb-1 form-check">
                    <label class="form-check-label">
                        <input asp-for="RememberMe" class="form-check-input" /> @Html.DisplayNameFor(a => a.RememberMe)
                    </label>
                </div>
                <div class="row-2">
                    <div class="col-8">
                        <a asp-action="Register" class="text-decoration-underline float-start mt-2" asp-route-returnurl="@ViewData["ReturnUrl"]">¿No tenes cuenta?</a>
                    </div>
                    <div class="col-4">
                        <button class="btn btn-danger" type="submit" value="Login">Ingresar</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}